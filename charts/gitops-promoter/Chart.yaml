apiVersion: v2
name: gitops-promoter
description: Wrapper Helm chart for gitops-promoter (argoproj-labs) — no official chart exists upstream.
type: application
version: 0.32.0
appVersion: "v0.32.0"
sources:
  - https://github.com/argoproj-labs/gitops-promoter
